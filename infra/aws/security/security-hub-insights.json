{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eunox.dev/schemas/aws/security-hub-insights.json",
  "title": "Eunox - AWS Security Hub custom insights (Sprint-1 OBS parity)",
  "description": "Custom Security Hub insights that group the CloudWatch Alarm-driven findings produced by `cloudwatch-alarms.yaml` into the same operational views as the Microsoft Sentinel rules in `infra/sentinel/analytic-rules.json`. Apply with: aws securityhub create-insight --cli-input-json file://<one of these objects>.",
  "insights": [
    {
      "Name": "Eunox - Capability denial spikes by agent/session",
      "GroupByAttribute": "ResourceId",
      "Filters": {
        "ProductName": [{ "Value": "CloudWatch", "Comparison": "EQUALS" }],
        "Title":       [{ "Value": "Eunox-DenialSpike", "Comparison": "PREFIX" }],
        "RecordState": [{ "Value": "ACTIVE", "Comparison": "EQUALS" }],
        "WorkflowStatus": [{ "Value": "NEW", "Comparison": "EQUALS" }]
      }
    },
    {
      "Name": "Eunox - Write attempts under read-only sessions",
      "GroupByAttribute": "ResourceId",
      "Filters": {
        "ProductName": [{ "Value": "CloudWatch", "Comparison": "EQUALS" }],
        "Title":       [{ "Value": "Eunox-WriteInReadOnly", "Comparison": "PREFIX" }],
        "RecordState": [{ "Value": "ACTIVE", "Comparison": "EQUALS" }]
      }
    },
    {
      "Name": "Eunox - Invalid capability token bursts",
      "GroupByAttribute": "ResourceId",
      "Filters": {
        "ProductName": [{ "Value": "CloudWatch", "Comparison": "EQUALS" }],
        "Title":       [{ "Value": "Eunox-InvalidTokenBurst", "Comparison": "PREFIX" }],
        "RecordState": [{ "Value": "ACTIVE", "Comparison": "EQUALS" }]
      }
    },
    {
      "Name": "Eunox - Kill-switch activations",
      "GroupByAttribute": "ResourceId",
      "Filters": {
        "ProductName": [{ "Value": "CloudWatch", "Comparison": "EQUALS" }],
        "Title":       [{ "Value": "Eunox-KillSwitchActivated", "Comparison": "PREFIX" }],
        "RecordState": [{ "Value": "ACTIVE", "Comparison": "EQUALS" }]
      }
    },
    {
      "Name": "Eunox - Token revocation spikes",
      "GroupByAttribute": "ResourceId",
      "Filters": {
        "ProductName": [{ "Value": "CloudWatch", "Comparison": "EQUALS" }],
        "Title":       [{ "Value": "Eunox-TokenRevocationSpike", "Comparison": "PREFIX" }],
        "RecordState": [{ "Value": "ACTIVE", "Comparison": "EQUALS" }]
      }
    }
  ],
  "guardDutyNotes": [
    "GuardDuty is enabled at the account level by Terraform's aws_securityhub_account resource (see infra/terraform/aws/main.tf).  It produces VPC- and DNS-level findings (e.g. UnauthorizedAccess:EC2/MaliciousIPCaller.Custom) for the EKS nodes hosting Eunox workloads — these complement, not replace, the application-level rules above.",
    "To correlate GuardDuty findings with Eunox workloads, tag the EKS cluster and node group with `product=eunox` (already done by the Terraform module) and create a Security Hub insight with GroupByAttribute=`ResourceTags.product` filter `EQUALS eunox`."
  ]
}
