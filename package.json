{
  "name": "@euno/monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Capability-Native Agent Governance System with Azure Integration",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @euno/common && npm run build -w @euno/capability-issuer && npm run build -w @euno/tool-gateway && npm run build -w @euno/agent-runtime && npm run build -w @euno/framework-adapters && npm run build -w @euno/cli && npm run build -w @euno/integration-tests",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces && rm -rf node_modules"
  },
  "keywords": [
    "agent-governance",
    "capability-based-security",
    "azure-integration",
    "decentralized-identity"
  ],
  "author": "EdgeObs",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
