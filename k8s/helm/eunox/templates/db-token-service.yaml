{{/*
db-token-service Deployment
*/}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eunox.fullname" . }}-db-token-service
  labels:
    {{- include "eunox.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-token-service
spec:
  replicas: {{ .Values.dbTokenService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: eunox-db-token-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: eunox-db-token-service
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: db-token-service
    spec:
      containers:
        - name: db-token-service
          image: "{{ .Values.dbTokenService.image.repository }}:{{ .Values.dbTokenService.image.tag }}"
          imagePullPolicy: {{ .Values.dbTokenService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.dbTokenService.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          envFrom:
            - configMapRef:
                name: {{ include "eunox.fullname" . }}-db-token-service-config
            {{- if .Values.dbTokenService.secretEnv }}
            - secretRef:
                name: {{ include "eunox.fullname" . }}-db-token-service-secret
            {{- end }}
          resources: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "eunox.fullname" . }}-db-token-service
  labels:
    {{- include "eunox.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-token-service
spec:
  type: {{ .Values.dbTokenService.service.type }}
  ports:
    - name: http
      port: {{ .Values.dbTokenService.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: eunox-db-token-service
    app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "eunox.fullname" . }}-db-token-service-config
  labels:
    {{- include "eunox.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-token-service
data:
  {{- range $key, $val := .Values.dbTokenService.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- if .Values.dbTokenService.secretEnv }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eunox.fullname" . }}-db-token-service-secret
  labels:
    {{- include "eunox.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-token-service
type: Opaque
stringData:
  {{- range $key, $val := .Values.dbTokenService.secretEnv }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}
